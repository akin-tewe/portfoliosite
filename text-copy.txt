NEIGHBORHOOD CASE STUDY — ALL TEXT COPY
========================================

═══ HERO ═══

Title: Neighborhood
Subtitle: Design System v0.1

Bio:
A component library and design system built from the ground up, with warmth, motion, and personality baked into every token. This case study covers the initial v0.1 conception and implementation. Neighborhood is actively under development, and the system continues to evolve beyond what is documented here.

Metrics:
- Role: Sole Designer · Developer
- Components: 18 Components · 11 Atomic · 7 Compositional
- Type: Design System · Documentation
- Status: v0.1 Shipped · Active Development

CTA Buttons:
- Documentation Site (links to nbhd.dev)
- GitHub (links to github.com/akin-tewe/neighborhood-ui)


═══ SHOWCASE: THE SYSTEM AT A GLANCE ═══

Section Label: The System at a Glance

Body:
Every component below is live — not a screenshot, not a mockup. These are the actual shipped npm components rendered on this page. Interact with them.

Cell Labels: Button, Badge, Toggle, Tabs, Input, Avatar, Switcher, Banner, Empty State


═══ TOKEN SYSTEM ═══

Section Label: Token System

Body:
Every visual decision is a CSS custom property. Components reference tokens exclusively — no hardcoded values. Six categories: typography, color, spacing, radius, elevation, and motion. The token system was finalized before any components were built.

Sub-label: Warm Stone Neutrals
Caption: 11 warm steps. Approachable, not clinical.

Sub-label: Extended Palette

Sub-label: Role-Based Radius
Radius items:
- Control — 8px (Buttons, inputs)
- Surface — 12px (Cards, panels)
- Pill — 9999px (Badges, chips)

Sub-label: Motion Tokens
Body: 4 easing curves · 4 durations. Click to compare bracket and landing selection patterns.
Tab labels (bracket variant): Overview, Guidelines, Tokens
Tab labels (landing variant): Overview, Guidelines, Tokens
Easing tokens: --ease-default, --ease-spring, --ease-pop, --ease-loop


═══ COMPONENTS ═══

Section Label: Components

Body:
18 components in two layers. Eleven atomic primitives handle individual interactions. Seven compositional components compose atoms into reusable patterns — this is where the systems thinking lives.

--- Button ---
Label: Button
Description: 4 variants · hover, active, focus, disabled, loading states

--- Setting Row ---
Label: Setting Row
Description: Appeared 10+ times on a single Notion page. Label + description left, any control right. The slot accepts any atom.

--- Property ---
Label: Property
Description: All three products display labeled data differently. One component API unifies them.
Sub-labels: Notion style, Stripe style, Discord style

--- Banner + Empty State ---
Label: Banner + Empty State
Description: 4 severity levels. Empty State uses a signature dashed border. Both support action slots.

--- Two-Repo Architecture ---
Label: Two-Repo Architecture
Body: Published as @neighborhood/ui. Doc site imports from the package, not local files — enforcing a clean API boundary.
Code block: $ npm install github:akin-tewe/neighborhood-ui

--- Accessibility ---
Label: Accessibility
Body: Every component has a visible focus ring via :focus-visible. Consistent spec: 2px solid sky-400, 2px offset. Every documented keyboard interaction works. Claims without implementation are worse than no claims.


═══ COMPOSITION ═══

Section Label: Composition

Body:
Atoms compose into patterns. Scroll to watch a settings panel assemble itself from individual components — the thesis made visible.

Assembly label: Building a settings panel...
Completion text: 3 atoms → 1 composed settings panel


═══ METHODOLOGY ═══

Section Label: Methodology

Body:
Structured audits of three shipping products — analyzing full-page layouts, mid-level patterns, and atomic elements. Three products chosen as a triangle: content tools, data dashboards, social platforms.

Product cards:
- Notion — 9 views
- Discord — 12 views
- Stripe — 8 views

Audit image captions (Notion):
- Settings preferences — the densest setting row page across all audits
- People settings — empty states, tabs, and invite input patterns
- Command palette — search overlay with categorized results
- Home — content-first layout with warm typography and sidebar navigation
- Share popover with input, permissions, and avatar row

Audit image captions (Discord):
- User popover — the richest component across all three audits
- Notification settings — toggle rows confirming setting row patterns
- Appearance — segmented controls and radio patterns for themes
- Dense real-time message layout with hover actions
- Server menu — dropdown with icons, dividers, destructive items

Audit image captions (Stripe):
- Payments — data table with typed cells, status badges, selection
- Business settings — form sections with grouped inputs
- Dashboard — metric cards, charts, and property displays
- Customer list with filters, search, and table composition
- Revenue metrics with chart and segment breakdown

Post-carousel body:
If a component appeared in all three, it was non-negotiable. Two appearances meant strong consideration. One appearance required high compositional value to earn inclusion. Setting rows, property displays, and banners all passed. Command palette and user profile card are documented for v0.2.


═══ TRADEOFFS & SCOPE ═══

Section Label: Tradeoffs

Body:
Every decision to include a component was also a decision to exclude others. The inventory is deliberately tight — 18 components with clear cross-product rationale over 40 from a checklist.

--- Same Need, Different Solutions ---
Settings architecture alone had three valid approaches — Notion uses a modal overlay, Discord does a full-page takeover, Stripe uses a card grid hub. Property display had three implementations. Destructive actions used red fill (Stripe) vs red text (Discord). Neighborhood documents the analysis and picks the right approach per context, not per preference.

--- Inclusion vs Exclusion ---
Toggle appeared in Notion (10+ instances) and Discord but not Stripe — included because two products confirmed universality. Switcher appeared only in Stripe but its compositional value (filtering data in place without navigating) earned inclusion as a distinctive pattern. Metric Card was excluded from v0.1 — rich and reusable, but too specific to data-dense dashboards for a system establishing its identity. Reaction badges (Discord-only) were excluded entirely: high personality, low cross-product applicability.

Section Label: Scope

In Scope — v0.1 Shipped:
- 11 Atomic: Avatar, Badge, Button, Checkbox, Divider, Input, Switcher, Select, Tabs, Toggle, Tooltip
- 7 Compositional: Banner, Block, Table, Empty State, Form Group, Menu, Property, Setting Row

Up Next — v0.2 Roadmap:
- Emerging patterns: card system, showcase surfaces, flat strip indicator
- New components: Command Palette, avatar color prop
- Dark mode via token architecture · messaging + e-commerce demos


═══ DEMOS ═══

Section Label: Demos

Body:
Three demo pages, each exercising a different composition context. The settings demo below uses 14 of 18 components. Switch tabs to see the content change.


═══ WHAT'S NEXT ═══

Section Label: What's Next

Body:
v0.1 shipped the foundation — but several patterns emerged during documentation site development that are actively shaping the next version.

--- Emerging Patterns ---
Label: Emerging Patterns
Body: Patterns observed during doc site construction that are candidates for formal components. Cards, showcase surfaces, and a flat strip indicator (the non-rounded sibling of Banner, purpose-built for unified card compositions) have all been documented and are being evaluated for extraction. Expanded usage guidelines and implementation guidance per component are also in progress.
Pills: Card System, Showcase Surface, Flat Strip, Usage Guidelines

--- v0.2 Roadmap ---
Label: v0.2 Roadmap
New Components:
- Metric Card (Stripe), Command Palette (Notion), Profile Card (Discord)
- Avatar color prop, card system, showcase surfaces
- Dark mode via token architecture

Emerging Patterns:
- Flat strip indicator, detailed usage + implementation guidelines
- Doc page layout abstraction
- Messaging + e-commerce demo pages

--- Selection Patterns ---
Label: Selection Patterns
Body: Two signature selection patterns have been established. Bracket (primary-400 vertical bars, spring animation) for primary navigation and tabs. Landing (translateY settle with inset shadow) for secondary nav and text-heavy lists. Both are documented with specific motion curves and use-case rules.

--- Responsive Infrastructure ---
Label: Responsive Infrastructure
Body: CSS-first responsive system with two breakpoints (768px tablet, 640px mobile). Components include their own mobile behavior — Setting Row wraps controls, Block hides descriptions, Switcher stacks vertically inside settings. Pages use layout classes and the CSS handles the rest.

Closing body:
Additional demo pages covering messaging and e-commerce contexts are planned to demonstrate wider compositional range. Dark mode implementation via the existing token architecture is also in scope — the CSS custom property foundation means it's a matter of defining alternate values, not restructuring components.


═══ BUILT WITH ═══

Section Label: Built With

Tech pills: Next.js 16, React 19, TypeScript, Tailwind CSS 4, CSS Custom Properties

Body:
Tokens use CSS custom properties — they work anywhere CSS works.
